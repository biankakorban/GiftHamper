@model CategoryDetailsViewModel

@*<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <h2>List of hampers</h2>
        </div>
    </div>
    @*<div class="row">
        <div class="col col-md-3 mb-2 btn btn-dark" type="button">
            @Html.ActionLink("Add a category", "Create", "Category")
        </div>

    </div>

    <div class="row table-responsive-sm">
        <table class="table">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Categories</th>
                    <th scope="col">Name</th>
                    <th scope="col"></th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Categories)
                {
                    <tr>
                        <td></td>
                        <td class="mt-2"> @item.Name</td>
                        <td class="btn btn-dark mt-2">@Html.ActionLink("Edit category name", "Edit", "Category", new { id = item.CategoryId })</td>
                        <td class="btn btn-dark mt-2 alert alert-danger" role="alert">@Html.ActionLink("Delete category", "Delete", "Category", new { id = item.CategoryId })</td>
                    </tr>

                }
            </tbody>
        </table>
    </div>*@

    @*<div class="row">
        <div class="col col-md-3 mb-2 btn btn-dark" type="button">
            @Html.ActionLink("Add a hamper", "Create", "Hamper")
        </div>

    </div>

    <div class="row table-responsive-sm">
        <table class="table">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Categories</th>
                    <th scope="col">Name</th>
                    <th scope="col"></th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Categories)
                {
                    <tr>
                        <td></td>
                        <td class="mt-2"> @item.Name</td>
                        <td class="btn btn-dark mt-2">@Html.ActionLink("Edit category name", "Edit", "Category", new { id = item.CategoryId })</td>
                        <td class="btn btn-dark mt-2 alert alert-danger" role="alert">@Html.ActionLink("Delete category", "Delete", "Category", new { id = item.CategoryId })</td>
                    </tr>

                }
            </tbody>
        </table>
    </div>

</div>*@